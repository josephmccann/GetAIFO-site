REPLIT HOTFIX — (1) BLOCK SECRET FILE PROBES + (2) IMPROVE AIRTABLE ERROR LOGGING + (3) FIX AIRTABLE 404 ROOT CAUSE

Context:
Vite + React client (client/) and Express-style server (server/). No new dependencies. Preserve current API behavior for the frontend.

PART 1 — SECURITY: BLOCK DOTFILE / SECRET PATH PROBES (URGENT)

Problem:
Logs show requests like GET /api/.env returning 200. This must return 404 and must never serve any file contents.

Changes:
1) In the Express server setup (server/index.ts or wherever app is created):
   Add a middleware early (before routes/static) that blocks requests containing:
   - "/.env"
   - ending with ".env"
   - "config.env"
   - any path segment starting with "." (dotfiles) under /api
   Return status 404 (or 403) with a plain response like "Not found".

2) Ensure the app does NOT serve any static files from an /api path.
   Static serving should only be for the built frontend assets, and API routes should be handled by explicit Express routes only.

3) Confirm after change:
   - GET /api/.env returns 404
   - GET /api/config.env returns 404
   - GET /api/stripe.ts returns 404
   - Existing API endpoints (POST /api/waitlist) still work.

PART 2 — AIRTABLE: LOG RESPONSE BODY WHEN AIRTABLE FAILS

Problem:
We only see Airtable status=404. We need the response body to know whether the base or table is wrong.

Changes:
In the Airtable request code (server/routes.ts or wherever Airtable POST happens):
- If Airtable response is not ok:
  - Read response text via await response.text()
  - Log: `Airtable error: status=<status> body=<text>`
- Do not return the Airtable body to the client. Keep generic error response.
- Keep existing response format to the client: { ok: false, error: "Unable to process your request right now. Please try again." }

PART 3 — AIRTABLE 404 ROOT CAUSE FIX (TABLE NAME)

Problem:
Airtable 404 is most commonly caused by the table name mismatch.

Changes:
- Confirm the table name being posted to is "Submissions".
- If the current Airtable base does not have a table named exactly "Submissions", either:
  A) Update Airtable table name to "Submissions" (preferred), OR
  B) Update code to match the actual table name (and use encodeURIComponent on table name if it includes spaces).

Deliverable checklist:
- /api/.env and similar paths return 404 (not 200)
- Airtable failure logs include status and body text
- Waitlist submission works when Airtable base/table are correctly configured
- No new dependencies